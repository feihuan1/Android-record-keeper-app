package com.example.recordkeeper

//these are top level, we can call it anywhere in other class

const val MY_CONSTANT = 42

val hello = "hello"

fun someFunc(name: String): String {
    return "hello $name"
}